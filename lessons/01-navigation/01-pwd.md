`pwd` means “print working directory”. It tells you the full path of the
directory your shell is currently using.

Start by running:

```bash
pwd
```

Shellforge starts you in `/home/student/workspace`. Every relative path you
type is interpreted from this directory.
